name: Create Discussion
on:
  push:
jobs:
  logLatestRelease:
    runs-on: ubuntu-latest
    steps:
      - uses: octokit/graphql-action@v2.x
        id: get_latest_release
        with:
          query: |
            mutation createDiscussionMutation {
              createDiscussion(input: {repositoryId: "R_kgDOI5vcDQ", categoryId: "DIC_kwDOI5vcDc4CUA42", title: "test", body: "body"}) {
                discussion {
                  id
                }
              }
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: "echo '${{ steps.get_latest_release.outputs.data }}'"
      - run: "echo '${{ fromJSON(steps.get_latest_release.outputs.data).createDiscussion.discussion.id }}'"
      - uses: octokit/graphql-action@v2.x
        id: add_label
        with:
          variables: |
            discussionId: ${{ fromJSON(steps.get_latest_release.outputs.data).createDiscussion.discussion.id }}
          query: |
            mutation($discussionId:String!) {
              addLabelsToLabelable(
                input:{
                  labelableId: $discussionId
                  labelIds: ["LA_kwDOI5vcDc8AAAABMOwhlg"]
                }
              ) {
                clientMutationId
              }
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
